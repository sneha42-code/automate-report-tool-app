import React from "react";

const AuthRightSection = ({ children }) => (
  <div className="auth-container medium-card auth-right-section">
    {children}
  </div>
);

export default AuthRightSection;
